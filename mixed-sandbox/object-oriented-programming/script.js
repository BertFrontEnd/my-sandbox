//---------------

// Cpu and Memory
/* import { cpu, memory } from './assets/module/cpu-and-memory.js';

console.log(cpu);
console.log(memory);

console.log(cpu.getAmount());
console.log(memory.getAmount()); */

//---------------

// Button and Modern Button
/* import { Button } from './assets/module/Button.js';
import { ModernButton } from './assets/module/ModernButton.js';

console.log(Button);
console.log(ModernButton);

const newButton = new Button(150, 40, 'lightgreen', 'Button').render();

const newButtonModern = new ModernButton(
  100,
  40,
  'lightblue',
  'Button',
  10,
).render(); */

//---------------
